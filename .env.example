# Production (Vercel) uses Neon. Omit locally to use embedded PGLite.
# DATABASE_URL=postgres://user:pass@host/db?sslmode=require

# Admin UI at /admin (required in production; at least 16 chars, not a placeholder)
ADMIN_PASSWORD=

# GitHub Action POST /api/cron/crawl (Authorization Bearer). Prefer 32+ random hex.
CRON_SECRET=
APP_URL=https://jobrow.vercel.app

# Public origin used in sitemap / JSON-LD
VITE_SITE_URL=https://jobrow.vercel.app
