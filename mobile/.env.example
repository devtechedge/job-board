# Public Jobrow API (Vercel). Default if unset.
EXPO_PUBLIC_API_URL=https://jobrow.vercel.app

# Local TanStack app. On a physical device use your machine LAN IP, not localhost.
# EXPO_PUBLIC_API_URL=http://localhost:8080
